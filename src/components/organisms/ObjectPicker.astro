<!-- <script>
  function createCircle(x, y) {
    const circle = new Konva.Circle({
      x: x,
      y: y,
      radius: 20,
      fill: "red",
      draggable: true,
    });

    // Event listener for dragend event
    circle.on("dragend", (e) => {
      const target = e.target;
      const targetPos = target.getAbsolutePosition();

      const targetX = target.getAbsolutePosition().x;
      const targetY = target.getAbsolutePosition().y;

      createCircle(targetX, targetY);
    });

    deckLayer.add(circle);
  }
</script> -->
